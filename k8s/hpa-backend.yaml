apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: awp-backend-hpa
  namespace: ai-workflow
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: awp-backend
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70